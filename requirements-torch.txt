# ================================================================
# PyTorch — installed separately with CUDA support
# Same versions as your current install
# CUDA 11.8 build works on:
#   - CPU only machines (CUDA is ignored automatically)
#   - GPU machines with CUDA 11.8 or higher
# ================================================================
torch==2.2.2+cu118
torchvision==0.17.2+cu118

# =======prompt=======================

We are working on a Pedestrian Violation Detection (PVD) System 
dockerization. Here is the exact state where we stopped:

── PROJECT STRUCTURE ──────────────────────────────────────────
PedestrianProject/
├── Dockerfile              ← Backend + ML (root level)
├── docker-compose.yml
├── .env
├── requirements.txt        ← JUST CREATED (pip packages only)
├── requirements-mim.txt    ← JUST CREATED (mmengine,mmcv,mmdet,mmpose)
├── requirements-torch.txt  ← JUST CREATED (torch+cu118, torchvision)
├── src/                    ← ML pipeline (READ ONLY)
├── configs/
├── backend/                ← FastAPI
└── frontend/
    ├── Dockerfile
    └── nginx.conf

── CURRENT PACKAGE VERSIONS (CONFIRMED WORKING) ───────────────
torch==2.2.2+cpu (currently cpu — docker will use cu118 build)
torchvision==0.17.2
mmcv==2.2.0 (installed from local Windows .whl — needs mim in Docker)
mmcv-lite==2.1.0 (conflict — needs to be removed)
mmpose==1.3.1
mmdet==3.3.0
mmengine==0.10.4
facenet-pytorch==2.6.0
opencv-python==4.8.1.78 (needs headless in Docker)
fastapi==0.109.0
motor==3.7.1
numpy==1.24.3

── CRITICAL ISSUES ALREADY IDENTIFIED ─────────────────────────
1. mmcv installed from Windows .whl — must use mim install in Docker
2. mmcv-lite and mmcv both installed — conflict, remove mmcv-lite
3. opencv-python must change to opencv-python-headless in Docker
4. torch needs cu118 build in Docker for CPU+GPU support
   (UNCONFIRMED — need to verify torch+cu118 works on CPU-only machine)
5. torchaudio removed — not needed in this project

── PENDING QUESTIONS TO ANSWER TOMORROW ───────────────────────
1. Does torch==2.2.2+cu118 work correctly on CPU-only machine?
   (Need web search confirmation before writing final Dockerfile)
2. Does mim install mmcv==2.2.0 work on Linux?
   (Cannot test today — presentation tomorrow)
3. Are the 3 requirements.txt files complete and correct?

── WHAT WE WILL DO TOMORROW ────────────────────────────────────
Step 1 → Confirm torch+cu118 works on CPU (web search answer)
Step 2 → Confirm mim install mmcv==2.2.0 works on Linux
Step 3 → Remove mmcv-lite conflict
Step 4 → Write final clean Dockerfile (backend+ML in one container)
Step 5 → Write final docker-compose.yml
Step 6 → Test build
Step 7 → Fix any errors that come up

── APPROACH ────────────────────────────────────────────────────
- One Docker image runs on BOTH CPU and GPU automatically
- Backend + ML pipeline → one container
- Frontend → separate container (already written)
- MongoDB → separate container
- Camera access is through browser — Docker does NOT need /dev/video0

Please search and confirm the torch+cu118 CPU question first,
then continue from Step 2 onwards. Do not skip any step.